# ── Python ──────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.pyo
.Python

# ── Virtual environments ─────────────────────────────
venv/
env/
.venv/
.env

# ── IDEs ─────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo

# ── OS ───────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Large binary / generated outputs ─────────────────
# Videos
*.mp4
*.avi
*.mov
!outputs/cv_annotated_output.mp4

# Images (generated outputs — keep source images if any)
outputs/*.png
outputs/*.jpg
outputs/frame.png

# Large HTML maps (Folium)
outputs/heatmap.html
outputs/deployment_map.html
# Note: heatmap_updated.html is tracked since it's only 50KB and required by app_v2.py

# Model weights
models/*.pt
models/*.pkl
*.pkl

# ── Data files ───────────────────────────────────────
data/raw/
# Large raw CSVs (100+ MB)
data/processed/cleaned_violations.csv
data/processed/impact_scored.csv
# training_features.csv (4 MB) — exclude too
data/processed/training_features.csv
# hex_aggregated.csv is only 60 KB — tracked in git

# Keep small JSON outputs (deployment, detections, chronic, recovery)
# Exclude large prediction JSON (5+ MB) — regenerate with: python phase2_prediction/predict.py
outputs/predictions.json
# feature_importance.png is tracked since it's small

# ── Streamlit cache ───────────────────────────────────
.streamlit/secrets.toml
cache/

# ── Jupyter ───────────────────────────────────────────
.ipynb_checkpoints/
*.ipynb
